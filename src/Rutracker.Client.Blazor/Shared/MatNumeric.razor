@typeparam T
@inherits InputNumber<T>

<div class="@CssClass mat-numericUpDownField mdc-text-field _mdc-text-field--upgraded mdc-text-field--with-leading-icon mdc-text-field--with-trailing-icon mdc-text-field--fullwidth mdc-text-field--fullwidth-with-leading-icon mdc-ripple-upgraded">
    <MatIcon Icon="@MatIconNames.Border_color" class="mdc-text-field__icon" />
    <InputNumber @bind-Value="@CurrentValue"
                 class="mdc-text-field__input"
                 style="padding: 20px 16px 18px 100px; text-align: left;" />

    <label class="mdc-floating-label" style="padding-top: 2px;">@Label</label>
    <div class="mdc-line-ripple"></div>
</div>

@code
{
    [Parameter] public string Label { get; set; }
}