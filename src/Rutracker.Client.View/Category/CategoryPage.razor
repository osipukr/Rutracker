@page "/categories/{Id:int}"

@inject PathHelper PageHelper

<Title>@PageHelper.GetPageTitle("Category")</Title>

<div class="Category-Root">
    <h3>CategoryPage</h3>
</div>

@code
{
    [Parameter] public int Id { get; set; }

    private CategoryDetailView _category;
    private ErrorModel _errorModel = ErrorModel.Create();
}
