version: "3.1"

services:
  blazor-server:
    image: blazor-server
    build: 
      context: .
      dockerfile: src/Rutracker.Server/Dockerfile
    environment:
      - ASPNETCORE_ENVIRONMENT=Production
    ports:
      - "5106:80"