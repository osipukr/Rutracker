version: "3.7"

services:
  blazor:
    image: blazor-server
    build: 
      context: .
      dockerfile: src/Rutracker.Server.WebApi/Dockerfile
    environment:
      - ASPNETCORE_ENVIRONMENT=Production
    ports:
      - "5106:80"