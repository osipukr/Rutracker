version: "3.7"

services:
  blazor:
    image: blazor-server
    build: 
      context: .
      dockerfile: src/Rutracker.Server/Dockerfile
    environment:
      - ASPNETCORE_ENVIRONMENT=Production
    ports:
      - "5106:80" 